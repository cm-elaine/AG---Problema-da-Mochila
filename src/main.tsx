import React from 'react'
import ReactDOM from 'react-dom/client'
import KnapsackAGApp from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KnapsackAGApp />
  </React.StrictMode>,
)